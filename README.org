#+title: emacs.conf

=keyboard layout, branch d02: base qwertz=

this is basically lee's emacs config .,.

[[file:./config.org][config.org]] is the main config file

[[file:./init.el][init.el]] is responsible for converting ~config.org~ to its elisp proper [[file:./config.el][config.el]]

[[file:./custom.el][custom.el]] is for storing all custom sets
